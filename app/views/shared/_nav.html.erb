<nav>
  <div class="navbar navbar-default navbar-fixed-top" role="navigation">

    <div class="navbar-header">
      <%= link_to "Bitsy", root_path, class: "navbar-brand" %> </a>
    </div>
    <a name="#top"></a>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Browse<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "All Products", products_path %></li>
            <li role="separator" class="divider"></li>

            <li class="dropdown-header">Categories</li>
            <% @categories.each do |category|  %>
              <li><%= link_to category.name.titlecase, "#{category_path(category.name)}" %></li>
            <% end %>
            <li role="separator" class="divider"></li>

            <li class="dropdown-header">Merchants</li>
            <% @merchants.each do |merchant| %>
            <li><%= link_to merchant.name.titlecase, "#{merchant_products_path(merchant.id)}" %></li>
            <% end %>
            <li role="separator" class="divider"></li>
          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if session[:user_id].nil?  %>
          <li><%= button_to "Login", login_path, method: :get, class: "btn btn-info navbar-btn" %></li>
          <li><%= link_to "Sign Up", new_user_path, class: "navbar-link" %></li>
        <% else %>
          <li><%= button_to "Logout", logout_path, method: :delete, class: "btn btn-info navbar-btn" %></li>
          <li><%= link_to "Store Dashboard", user_path(session[:user_id]), class: "navbar-link" %></li>
        <% end %>
        <li class="navbar-text cart-number"><%= current_order.order_items.size %></li>
        <li class="navbar-link"><%= link_to "", cart_path, class: "glyphicon glyphicon-shopping-cart cart" %></li>
      </ul>
      </div>
    </div>
  </div>
</nav>
