<div class="page-header">
  <h1>CHECKOUT</h1>
</div>

<%= render "display_nonpending_order_items" %>

<%= link_to "<< Back to cart", cart_path %>

<%= render "display_shipping_estimate" if @shipping %>
<%= render "shipping_estimate_form" unless @shipping %>

<%= render "buyer_info_form" %>
