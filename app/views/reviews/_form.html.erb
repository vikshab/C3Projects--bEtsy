<%= form_for([@product, @product.reviews.build]) do |f| %>
  <%= f.label :body %><br>
  <%= f.text_area :body %><br>
  <br>
  <%= f.label :body, "Comment" %><br>
  <%= f.text_area :body %><br>
  <br>
  <%= f.submit "Submit" %>
<% end %>
